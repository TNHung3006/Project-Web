@model Hung_Tran_Ngoc_66131218_Web_QLBH.Models.NhaCC

@{
    ViewData["Title"] = "Cập nhật nhà cung cấp";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-xl-10">
            <div class="card shadow border-0 rounded-3">
                <div class="card-header bg-warning bg-gradient text-dark p-3">
                    <h5 class="mb-0 fw-bold"><i class="fas fa-edit me-2"></i>CẬP NHẬT NHÀ CUNG CẤP</h5>
                </div>

                <div class="card-body p-4 bg-light">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="MaNCC" />

                        <div class="row g-4">
                            <div class="col-md-6">
                                <div class="card h-100 border-0 shadow-sm">
                                    <div class="card-header bg-white fw-bold text-primary border-bottom-0">
                                        <i class="fas fa-info-circle me-1"></i> Thông tin chung
                                    </div>
                                    <div class="card-body">
                                        <div class="form-floating mb-3">
                                            <input asp-for="TenNCC" class="form-control" />
                                            <label asp-for="TenNCC">Tên Nhà Cung Cấp</label>
                                            <span asp-validation-for="TenNCC" class="text-danger"></span>
                                        </div>

                                        <div class="form-floating mb-3">
                                            <input asp-for="DienThoaiNCC" class="form-control" />
                                            <label asp-for="DienThoaiNCC">Số điện thoại</label>
                                            <span asp-validation-for="DienThoaiNCC" class="text-danger"></span>
                                        </div>

                                        <div class="form-floating">
                                            <input asp-for="EmailNCC" class="form-control" />
                                            <label asp-for="EmailNCC">Email</label>
                                            <span asp-validation-for="EmailNCC" class="text-danger"></span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="col-md-6">
                                <div class="card h-100 border-0 shadow-sm">
                                    <div class="card-header bg-white fw-bold text-success border-bottom-0">
                                        <i class="fas fa-map-marker-alt me-1"></i> Địa chỉ
                                    </div>
                                    <div class="card-body">
                                        <div class="form-floating mb-3">
                                            <input asp-for="DiaChiNCC" class="form-control" />
                                            <label asp-for="DiaChiNCC">Địa chỉ cụ thể</label>
                                            <span asp-validation-for="DiaChiNCC" class="text-danger"></span>
                                        </div>

                                        <div class="mb-3">
                                            <label class="form-label small text-muted fw-bold">TỈNH / THÀNH PHỐ</label>
                                            <select id="ddlTinh" class="form-select mb-2" asp-items="ViewBag.Tinhs">
                                                <option value="">-- Chọn Tỉnh / Thành phố --</option>
                                            </select>
                                        </div>

                                        <div class="mb-3">
                                            <label asp-for="MaXa" class="form-label small text-muted fw-bold">XÃ / PHƯỜNG</label>
                                            <select id="ddlXa" asp-for="MaXa" class="form-select border-success" asp-items="ViewBag.Xas">
                                                <option value="">-- Chọn Tỉnh trước --</option>
                                            </select>
                                            <span asp-validation-for="MaXa" class="text-danger"></span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="d-flex justify-content-end gap-2 mt-4 pt-3 border-top">
                            <a asp-action="Index" class="btn btn-secondary px-4">
                                <i class="fas fa-arrow-left me-1"></i> Hủy bỏ
                            </a>
                            <button type="submit" class="btn btn-warning px-4 fw-bold shadow-sm">
                                <i class="fas fa-check-circle me-1"></i> Lưu thay đổi
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            $("#ddlTinh").change(function () {
                var maTinh = $(this).val();
                $("#ddlXa").empty();

                if (maTinh) {
                    $("#ddlXa").append('<option value="">Đang tải...</option>');
                    $.ajax({
                        url: '/NhaCCs/GetXaByTinh',
                        type: 'GET',
                        data: { maTinh: maTinh },
                        success: function (data) {
                            $("#ddlXa").empty();
                            $("#ddlXa").append('<option value="">-- Chọn Xã / Phường --</option>');
                            $.each(data, function (i, item) {
                                $("#ddlXa").append('<option value="' + item.maXa + '">' + item.tenXa + '</option>');
                            });
                        },
                        error: function () {
                            alert("Lỗi tải danh sách xã.");
                            $("#ddlXa").empty();
                        }
                    });
                } else {
                    $("#ddlXa").append('<option value="">-- Chọn Tỉnh trước --</option>');
                }
            });
        });
    </script>
}