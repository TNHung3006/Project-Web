@model Hung_Tran_Ngoc_66131218_Web_QLBH.Models.GianHang

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>GianHang</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            @* MaGH là khóa chính, phải được ẩn để gửi về Controller *@
            <input type="hidden" asp-for="MaGH" />

            <div class="form-group">
                <label asp-for="TenGH" class="control-label"></label>
                <input asp-for="TenGH" class="form-control" />
                <span asp-validation-for="TenGH" class="text-danger"></span>
            </div>

            @* PHẦN ĐÃ SỬA: Thay thế input MaXa bằng Dropdown List *@
            <div class="form-group">
                <label asp-for="MaXa" class="control-label">Xã/Phường</label>
                @* Sử dụng <select> và asp-items="ViewBag.Xas" (đã load trong Controller).
                Asp.Net Core MVC sẽ tự động chọn MaXa hiện tại của Model.GianHang *@
                <select asp-for="MaXa" class="form-control" asp-items="ViewBag.Xas">
                    <option value="">-- Chọn Xã/Phường --</option>
                </select>
                <span asp-validation-for="MaXa" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}