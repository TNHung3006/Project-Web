@model Hung_Tran_Ngoc_66131218_Web_QLBH.Models.CTBH

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Chi Tiết Bán Hàng</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            @* PHẦN MỚI: Xử lý MaDBH (Mã Đơn Bán Hàng - Khóa chính 1) *@
            <div class="form-group">
                <label asp-for="MaDBH" class="control-label">Mã Đơn Bán Hàng</label>
                @* Dùng asp-items để liên kết với ViewBag.DonBanHangs *@
                <select asp-for="MaDBH" class="form-control" asp-items="ViewBag.DonBanHangs">
                    <option value="">-- Chọn Đơn Bán Hàng --</option>
                </select>
                <span asp-validation-for="MaDBH" class="text-danger"></span>
            </div>

            @* PHẦN ĐÃ SỬA: Xử lý MaSP (Mã Sản Phẩm - Khóa chính 2) *@
            <div class="form-group">
                <label asp-for="MaSP" class="control-label">Sản Phẩm</label>
                @* Thay thế input bằng Dropdown List dùng ViewBag.SanPhams *@
                <select asp-for="MaSP" class="form-control" asp-items="ViewBag.SanPhams">
                    <option value="">-- Chọn Sản Phẩm --</option>
                </select>
                <span asp-validation-for="MaSP" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="SLB" class="control-label"></label>
                <input asp-for="SLB" class="form-control" />
                <span asp-validation-for="SLB" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="DGB" class="control-label"></label>
                <input asp-for="DGB" class="form-control" />
                <span asp-validation-for="DGB" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}